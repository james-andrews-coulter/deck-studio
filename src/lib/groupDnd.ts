/** Sentinel id used as the droppable id for the Ungrouped bucket. */
export const UNGROUPED_DROP_ID = '__ungrouped__';

/** Prefix used on dnd-kit draggable ids for group-header-level reordering. */
export const GROUP_HEADER_PREFIX = 'groupheader:';

/** Prefix used on dnd-kit droppable ids for group drop zones. */
export const GROUP_DROP_PREFIX = 'group:';
